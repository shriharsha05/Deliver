var app = new Framework7({
  root: '#app',
  // App Name
  name: 'Deliver',
  // App id
  id: 'deliver.paper.app',
  // Enable swipe panel
  panel: {
    swipe: 'left',
  }
});

var mainView = app.views.create('.view-main');